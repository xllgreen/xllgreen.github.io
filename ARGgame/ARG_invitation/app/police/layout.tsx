import type {Metadata} from "next";
export const metadata:Metadata={title:"临川公安｜线索协查档案",description:"临川市公安局虚构线索协查页面"};
export default function Layout({children}:{children:React.ReactNode}){return children}
