import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "恒慕婚姻家庭服务集团｜让幸福如期而至",
  description: "恒慕婚姻家庭服务集团官方网站。",
};

export default function HengmuLayout({ children }: { children: React.ReactNode }) {
  return children;
}
